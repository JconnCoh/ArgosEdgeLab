# R18H pre-full-KLARF scribe work plan — 2026-09-03

Classification: `PENDING_GATE`

## Completed and retained

- R18F is the unchanged frozen predecessor.
- The R18F four-member blind cohort passed visual review: four exact visible
  scribes and no blind acquisition reused for tuning.
- The R18G failure-first development cohort produced three exact visible
  strings, one correct no-scribe localization hold, and one image-first
  `T`/`7` ranking defect.
- Local diagnostic R18H fixes that defect with a generic, all-body-label
  run-structure consensus tie-break.  Slot08 is image-first
  `1478T161SUG7`; checksum remains verifier-only.
- R18H fast gates pass 9 established visible regressions, 3 R18G visible
  development cases, and 465 leave-one-physical-identity-out references with
  zero previously correct references harmed.

R18H remains local and review-only.  It has not been pushed, packaged,
published, activated, or run against a full KLARF tree.

## Mandatory work before a full KLARFExport run

1. Freeze and publish R18H only after explicit `PUBLISH` authority and its
   applicable local/package/route gates.  Run no retry and preserve R18G as
   terminal evidence.
2. Run another small failure-first validation batch drawn from wafers the
   current installed scribe reader could not read.  Include difficult POST2,
   blank installed crops caused by displaced scribes, and varied BF/DF
   appearance.  Ambiguous localization remains an operator-visible hold.
3. Read-only inventory and inspect all available scribe evidence in this exact
   operator-supplied response folder before full-corpus execution:

   `\\shm-cifs\Department\DE-1302_FAB_BE_Engineering\60_Saw_VI_Sort\600_General\Joshua.conn\AVI_Images\Argos\Uploads\InspectionRevs\ProjectPortalRO\responses\Lot_62629-401\62629-401_20260902002921`

   Treat this as a mandatory suspected-resist/odd-appearance validation cohort.
   First enumerate the exact contained acquisition/slot and existing scribe
   evidence paths without changing the folder.  Then freeze a bounded
   development/independent-validation split before reading image bytes.  Test
   localization, enhancement/polarity handling, segmentation, character
   ranking, confidence, and hold behavior across every represented scribe.
   Do not assume resist is present until the returned evidence establishes the
   appearance regime.
4. Resolve missing body-label coverage `I/O/V/Y` when exact examples exist, or
   preserve explicit coverage holds when they do not.  Lot/checksum knowledge
   may locate and verify candidate examples but may not select image-first
   glyphs.
5. Confirm the generic similarity safeguards on additional close character
   families.  Review every structural-arbitration event and every low-margin
   appearance result; do not limit analysis to `T`/`7`.
6. Freeze full-run acceptance criteria before launch: exact-string controls,
   no false string from blank/no-scribe crops, explicit displaced-scribe and
   missing-reference holds, review artifacts for failures, and no automatic
   identity acceptance or reference admission.
7. Only after those gates pass, run the full KLARFExport corpus in review-only
   mode with bounded outputs and a failure-first analysis queue.  Group failures
   by localization, appearance regime/channel, segmentation, missing label,
   close-character arbitration, checksum disagreement, and confidence/hold
   reason so detector adjustments can be evaluated against frozen regression
   cohorts before any successor run.

## Preserved authority

This plan grants no image read, source hash, portal/JBOD action, publication,
activation, identity acceptance, reference admission, hold clearance, XML,
training, production, source mutation, or wafer action.  Each external or
image-reading step retains its existing explicit gate.
