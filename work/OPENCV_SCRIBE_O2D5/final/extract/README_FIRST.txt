ARGOS O2D5 — ONE REVIEW-ONLY SLOT16 DEVELOPMENT RUN

Run only on JBOD A1025645101 from a fresh D:\O2D5 extraction.
Right-click RUN_O2D5.cmd and run as administrator once.

The launcher first performs an exact non-mutating preflight. It then runs the
frozen OpenCV scribe engine once against the exact frozen Slot16 BF/DF pair.
All work, output, and durable local result bytes remain on D:. The launcher
does not restart or change any task/process and does not activate the provider.

The exact local result is:
D:\A2\x\O2D5R_20260825T190855Z_54B4C08C.zip

The script also creates one host-authentic signed review-only response in the
already-installed response-sender outbox. If that outbound route fails, the
D-local result remains authoritative for manual return. Do not rerun O2D5.

Do not run O2D4, JEO1, CDM1, CDO1, or O2A2. DFLY3005 is not Argos and is
excluded. No XML, training, production routing, source mutation, deletion,
wafer action, task restart, process restart, or hold clearance is authorized.
