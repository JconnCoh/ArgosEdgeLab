# OpenCV OCV-02 O2A2 direct observation frozen and ready — 2026-08-25

Disposition: `PENDING_GATE`

## Outcome

The operator-authorized bounded `DIRECT_ADMIN_READ_ONLY` observation package
is frozen locally as O2A2. Its exact ZIP is
`work/OPENCV_SCRIBE_O2A2/final/ARGOS_O2A2.zip`, 8,816 bytes, SHA-256
`A60926D0EC26BB44B11B47AB70023EC72C08E4F19CE5DA97431CA5212C535C47`.
The committed exact-copy final package gate is
`work/OPENCV_SCRIBE_O2A2/O2A2_FINAL_PACKAGE_GATE.json`, SHA-256
`45C7D1AF71AF028BA017F4C9331CADE146F523C4793F41B8A76DDBA7DF811766`.

The exact ZIP contains five files. Extraction reproduced every declared byte
count and SHA-256. The extracted Windows PowerShell 5.1 entrypoint passed its
harness gate, wrapper gate, rehearsal preflight, and laptop-refusal control.
The package-shaped ZERO, ONE, and MANY audit cases passed. No test or gate read
image bytes or mutated a task, process, installed file, queue, ledger, source,
wafer, or hold.

## Withdrawn local attempts

O2A1 is withdrawn because its ZERO case dereferenced an absent
`Measure-Object.Sum` boundary. O2A2 test harness R1 is withdrawn because it
relied on implicit collection member enumeration. O2A2 test harness R2 is
withdrawn because it used a parenthesized `if` as an argument expression.
O2A2 build R1 is withdrawn because its expected laptop-refusal stderr was
promoted to a terminating `NativeCommandError`. Their exact roots and evidence
remain preserved and none contacted JBOD. The O2A2 audit package bytes passed
the corrected R3 test and R2 builder and were not changed by those harness
corrections.

## Authority and unresolved prerequisite order

The next action is publication of exactly the frozen O2A2 ZIP to the
engineering `InspectionRevs` share, after a clean committed branch with
matching local/origin tips is proved. Publication authorizes one operator run
only. The run installs nothing, starts/stops/restarts no task or process,
changes no queue or ledger, reads no image bytes, and performs no source or
wafer action. It writes evidence only to fresh `C:\O2A2` and returns the short
`O2A2R.zip` evidence leaf.

`REQ_O2D4` remains non-reusable and must not be retried. No corrected portal
successor may be published until the direct observation resolves O2D4's exact
pending/processed/ledger/work, response-outbox/sent, D-work/output, task,
process, and `X:` state. Slot16 is not frozen. Slot17 has not started. Slots
22-25 remain unseen. The live provider remains disabled. The healthy processor,
`SCRIBE_REFERENCE_COVERAGE_HOLD`, all earlier `PENDING_GATE` records, and every
existing hold remain unchanged. XML, training, production routing, deletion,
source mutation, and wafer action remain unauthorized.
