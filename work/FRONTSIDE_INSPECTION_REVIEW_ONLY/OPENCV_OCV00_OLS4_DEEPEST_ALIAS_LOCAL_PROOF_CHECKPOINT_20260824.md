# OpenCV OCV-00 OLS4 Deepest-Alias Local Proof Checkpoint — 2026-08-24

Disposition: `PENDING_GATE`

## Corrected inventory rule

OLS3 remains valid signed evidence but remains `HOLD_INCOMPLETE`; its 40
count-only unsafe-path skips cannot be used as a complete inventory baseline.
The corrected generic provider anchors its process-local FileSystem alias at
the exact requested subtree. All enumeration I/O uses that short alias.
Canonical full paths are retained only as provenance strings and are never
passed to file, hash, OpenCV, or child-process I/O. Every rejected child must
first produce one bounded identity row with canonical and alias budgets plus
an enumerated reason. An unchanged existing source component over 80
characters is advisory, not grounds for silently dropping the source.

## Windows PowerShell 5.1 local proof

The frozen local gate is
`work/OPENCV_OLS4/OLS4_DEEPEST_ALIAS_PROVIDER_GATE.json`, SHA-256
`791829F3EFE668B10FD6DB6CD6847F375556790204C8A9D811224627AD309396`.
ZERO, ONE, and MANY inventories all returned `COMPLETE` with zero skips. A
canonical path whose effective length exceeded 230 remained provenance-only
while alias I/O stayed below 200. Unsafe alias and filesystem-impossible
cases returned exact identity holds, a missing subtree failed closed, and the
alias was removed after an injected post-alias failure.

The preserved local fixture is `C:\O4F`; it contains four zero-byte metadata
test files. No JBOD endpoint was contacted, no image bytes were read, no
source hash was calculated, and no OpenCV image-processing code was run.

## Remaining OCV-00 gate

Live request publication remains unauthorized. The next step, only after
explicit operator authority, is one separately frozen metadata-only JBOD
request for `D:\KLARFExport\PatternedFront\Lot_62619-433` using this exact
provider design. The signed result must be `COMPLETE` with zero skipped-path
rows before source hashing is considered. OLS3's exact ten Slots 16–25 BF/DF
pairs remain frozen but do not waive the broader inventory hold.

The migration order remains OCV-00 inventory/baselines, OCV-01 provider
platform, OCV-02 full-area scribe search, OCV-03 independent 360-degree
perimeter/notch/global pose, and OCV-04 reusable fiducials. The healthy
processor and every existing FS15, notch, map, pose, fiducial, composite,
registration, defect, reviewer, XML, training, production, deletion, and
wafer-action hold remain unchanged.
