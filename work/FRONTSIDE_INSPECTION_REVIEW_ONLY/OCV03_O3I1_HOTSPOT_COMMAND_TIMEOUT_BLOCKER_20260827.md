# OCV-03 O3I1 Hotspot Inventory Command Timeout / Operator Console Blocker — 2026-08-27

Disposition: `PENDING_GATE`

The operator reported the existing JBOD layer visible. The bounded transport
inventory still found the exact established RustDesk forward, and one fresh
short hostname gate returned exact JBOD `A1025645101`. No new session was
opened and no window geometry was changed.

The frozen metadata-only observation intent passed the recovery-intent gate,
and its exact pre-action contract passed zero recurrence. The exact command is
`work/OPENCV_EDGE_NOTCH_O3D1/O3I1_HOTSPOT_METADATA_INVENTORY_COMMAND.ps1`,
SHA-256 `3902E54F3EE8DFC5039040AAEC12AB3E63B6768C88B432E4C8F23743BB06B7FD`,
with 1,934 characters under the 2,048-character direct-command bound. It uses
the installed `JBOD_KLARF_EXPORT` config mapping, bounds traversal to 20,000
entries and depth 8, refuses reparse traversal, returns metadata only, and does
not read or hash image bytes.

The one authorized command attempt returned no exact nonce, command hash,
PASS state, or result before its 50-second boundary. It was not retried. This
timeout is not inventory evidence: exact lot presence, directory count, BMP
leaf count, path safety, and cohort eligibility remain unknown. Remote command
completion is also unknown. The exact blocker is
`work/OPENCV_EDGE_NOTCH_O3D1/O3I1_HOTSPOT_INVENTORY_COMMAND_TIMEOUT_BLOCKER.json`,
SHA-256 `94978D7A5FECE6C13E63A81CBEB5574A27F35264543B1177AB83B4C4235ED8ED`.
Checkpoint pre-action SHA-256 is
`FE2CCEC3D383A4C9B5651BFFF7C0833228F7FE3EB91347E04EA04D116E90A423`.

Operator action required: without typing, clicking, accepting a dialog, or
changing window state, report whether the visible JBOD layer currently shows
a PowerShell parse/runtime error, a paste-confirmation dialog, an ordinary
prompt, or no visible console. Only if a parse/runtime error is visibly present
may the existing bounded `CaptureConsoleText` action collect that already-
visible text. Do not accept a paste dialog, repeat Enter, or retry the same
command namespace. Leave both sessions open.

O3D1 remains diagnostic-only fail-closed evidence. The OCV-02 four-of-four
ambiguity/reference/localization/identity hold, Slot25 metadata-disclosed
qualification, `lot62631586FrontGuiRecovery` `PENDING_GATE`, every
map/pose/fiducial/alignment prerequisite, O2D14 withdrawal, and DFLY3005
exclusion remain unchanged. The live provider remains disabled and the
protected processor remains untouched. Authority remains review-only with
training, XML, and production eligibility false.
