# OCV-02 parallel offline scribe draft authority — 2026-09-01

Disposition: `DIAGNOSTIC_ONLY`

The operator explicitly authorizes configuration-selected OpenCV scribe
deciphering development in parallel with the isolated OCV-03 targeted backside
work. This overrides only the prior phase-order prohibition on beginning
OCV-02 development before OCV-03 completes. It does not change any live,
publication, activation, identity, hold, XML, training, or production
authority.

This branch-local lane is confined to
`codex/opencv-scribe-deciphering` in the sibling worktree
`C:\Users\joshua.conn\Desktop\ArgosDev\ArgosEdgeLab-scribe-opencv`, based at
commit `ec97b70787181971ee93d28123ca7fe161a0b2a7`. Work is an offline draft
mutation using already-frozen local scribe evidence only. The active backside
detector/provider files, processor integration entrypoints, package schemas,
GUI importer, XML output, source images, and all JBOD/RustDesk/Project Portal
state are outside this lane.

The immutable V1R5 engine and its signed Slot22-Slot25 assessment remain
regression evidence. The recorded failure is four of four independent members
returning `SCRIBE_M12_AMBIGUOUS_MULTIPLE_VALID`, zero automatic identity
acceptance, and broad-texture image-first strings `FFFFFFFFFFF7`, while
reference-coverage and automatic-localization holds remain active. A successor
must preserve the accepted 15/15 recognition and four duplicate-view controls,
must not infer identity from filenames, slots, checksums, or proposal agreement,
and must retain explicit holds whenever localization or reference coverage is
unqualified.

Authority remains: `reviewOnly=true`, `trainingEligible=false`,
`xmlEligible=false`, `productionEligible=false`, live provider disabled,
protected processor untouched, no hold clearance, no source mutation or
deletion, and no task/process/queue/ledger action. No publication or activation
is authorized from this branch. Any later integration must use an explicit
controlled merge after provider-local review and must mechanically prove that
the active backside provider files and shared processor entrypoints are
unchanged.

Exact next action: create only a fresh provider-local OpenCV scribe successor
and local gate in a new namespace. Use the frozen V1R5 provider, accepted
recognition gates, and signed four-member ambiguity assessment as immutable
inputs. Stop before editing any shared processor entrypoint or package schema.
