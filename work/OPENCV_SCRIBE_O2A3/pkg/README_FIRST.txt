ARGOS O2A3 — ONE READ-ONLY SLOT16 SCRIBE-METADATA OBSERVATION

Run only on JBOD A1025645101 from a fresh D:\O2A3 extraction.
Right-click RUN_O2A3.cmd and run as administrator once.

The launcher first performs an exact non-mutating preflight. It then reads only
the exact current Slot16 catalog row, proposal JSON, multi-channel reader
summary or hold JSON, and hashes of the exact installed scribe caller, helper,
accepted reader, polarity code, reference manifest, and healthy processor
runner. It copies only bounded JSON evidence into D:\A2\x and creates one
signed review-only response through the already-installed response sender.

It does not open, hash, copy, decode, or return image files. It does not start,
stop, or restart any task/process; change any installed file, queue, ledger,
provider, source, wafer, or hold; or grant identity, XML, training, or
production authority. The healthy processor PID and creation time must remain
unchanged across the observation. O2A3 is one-attempt only.

Local result:
D:\A2\x\O2A3R_20260825T195521Z.zip

Do not rerun O2D5. Do not run O2D4, JEO1, CDM1, CDO1, or O2A2. DFLY3005 is a
Rudolph tool, not Argos, and is excluded. Slots22-25 remain unseen;
SCRIBE_REFERENCE_COVERAGE_HOLD and every existing hold remain in force.
